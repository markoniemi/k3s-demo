#spring:
#    cloud:
#        consul:
#            host: ${CONSUL_HOST:localhost}
#            discovery:
#                prefer-ip-address: true
spring:
  cloud:
    kubernetes:
      enabled: false
      discovery:
        enabled: false
